---
marp: true
theme: worksheet
paginate: false
---

<div class="sheet-head">
<div class="kicker">Рабочий лист · Урок 5.2</div>

# Журнал оценок 📓

<div class="fields"><span>Имя:</span><span>Класс:</span><span>Дата:</span></div>
</div>

## 1 · Что делает команда

Соедини команду со списками с тем, что она делает (впиши букву справа).

| Команда | | Что делает |
|---|---|---|
| `std::vector<int> v;` | ___ | **А** — добавляет число в конец |
| `v.push_back(5);` | ___ | **Б** — создаёт пустой список чисел |
| `v.size()` | ___ | **В** — берёт элемент по номеру |
| `v[0]` | ___ | **Г** — сколько элементов в списке |

## 2 · Что выведет программа

Запиши, что появится на экране.

```cpp
std::vector<int> v;
v.push_back(4);
v.push_back(8);
std::cout << v.size() << " " << v[1];
```

<div class="lines sm"></div>

## 3 · Журнал сломался — почини его 🔧

В коде **две ошибки**. Найди их и запиши исправленные строки.

```cpp
std::vector<int> ocenki;
ocenki.push_back 5;
std::cout << ocenki[1];   // оценка в журнале только одна
```

<div class="lines sm"></div>
<div class="lines sm"></div>

---

## 4 · Заводим журнал 📓

Напиши программу: создай список оценок, добавь три оценки и выведи, **сколько** оценок в журнале.

<div class="box lg" data-label="school.cpp"></div>

## 5 · Печатаем все оценки 🖨️

Напиши программу, которая перебирает журнал циклом `for` и выводит **все** оценки в строку через пробел.

<div class="box lg" data-label="school.cpp"></div>

## 6 · Средний балл класса <span class="star">★ со звёздочкой</span>

Напиши программу: сложи все оценки журнала, раздели на их количество и выведи **средний балл** с дробной частью.

<div class="box lg" data-label="school.cpp"></div>
