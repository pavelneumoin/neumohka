---
marp: true
theme: worksheet
paginate: false
---

<div class="sheet-head">
<div class="kicker">Рабочий лист · Урок 3.1</div>

# Игра задаёт вопросы 🎮

<div class="fields"><span>Имя:</span><span>Класс:</span><span>Дата:</span></div>
</div>

## 1 · Правда или ложь

Для каждого сравнения напиши, что получится: `1` (true) или `0` (false).

| Сравнение | Ответ (0 или 1) |
|---|---|
| `90 > 50` | ___ |
| `7 == 7` | ___ |
| `3 < 1` | ___ |
| `10 != 10` | ___ |
| `50 >= 50` | ___ |

## 2 · Что выведет программа

Игрок набрал 80 очков. Что напечатается на экране?

```cpp
int score = 80;
std::cout << (score >= 100);
```

<div class="lines sm"></div>

## 3 · Почини код 🔧

Программа должна проверить, **равно ли** здоровье нулю. В коде ошибка — найди и исправь.

```cpp
int hp = 0;
std::cout << (hp = 0);
```

<div class="lines sm"></div>

---

## 4 · Хватает ли монет 🪙

Меч стоит **100** монет. Напиши программу: заведи переменную `coins`, проверь, хватает ли на меч, и выведи результат (1 или 0).

<div class="box lg" data-label="game.cpp"></div>

## 5 · Открыт ли уровень 🔓

Уровень открывается с **3** звёзд. Напиши программу: спроси у игрока число звёзд через `std::cin` и выведи, открыт ли уровень.

<div class="box lg" data-label="game.cpp"></div>

## 6 · Рекорд побит? <span class="star">★ со звёздочкой</span>

Старый рекорд — **250** очков. Спроси новый результат и выведи **две** проверки: рекорд побит (новый больше) и рекорд повторён (новый равен старому).

<div class="box lg" data-label="game.cpp"></div>
